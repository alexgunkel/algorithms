// merge_sort.go
package sort

func merge_sort(sortable *[]int64) {
	input := *sortable
	length := len(input)
	if 1>=length {
		return
	}

	sortable = append(sort)
}

func sort(sortable []int64, start int, end int) {
	
}

func merge(sl *[]int64, start int, middle int, end int) {
	
}
